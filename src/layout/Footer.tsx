import { NewsLetter, FooterComponent } from "@/layout";

export const Footer = () => {
  return (
    <footer className="relative z-10">
      <NewsLetter />
      <FooterComponent />
    </footer>
  );
};
